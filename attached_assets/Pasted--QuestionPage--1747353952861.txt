 المشاكل والتعديلات المطلوبة في صفحة السؤال (QuestionPage):
✅ المؤقت لا يتوقف بعد انتهاء الوقت لجميع الفرق:

يجب أن لا يتم إعادة تشغيل المؤقت إذا انتهت الأدوار (مثلاً إذا كان هناك فريقان، فقط دورتين).

عند انتهاء وقت الفريق الثاني، يجب أن يتوقف المؤقت ولا يتم استدعاء moveToNextTeam() مرة أخرى.

✅ زر "تبديل الدور" يجب أن يظهر فقط بعد عرض الإجابة:

الزر حاليًا ظاهر دائمًا، يجب عرضه فقط بعد الضغط على "عرض الإجابة".

✅ عند الضغط على "تبديل الدور" يدويًا، يجب أن لا يتم إعادة تشغيل المؤقت إلا مرة واحدة فقط.

✅ المؤقت يجب أن يبدأ تلقائيًا عند فتح صفحة السؤال لأول مرة.

✅ بعد الإجابة، يتم العودة لصفحة اللعب ولكن الدور لا يتغير فعليًا هناك.

يجب التأكد أن API /update-team تعمل وتقوم بتحديث currentTeamIndex في قاعدة البيانات.

ويجب أن يتم جلب هذا التحديث في صفحة اللعب.

✅ يجب إرسال categoryId وdifficulty مع كل إجابة بشكل صريح (تم تنفيذه في الكود الحالي).

✅ إذا انتهى الوقت ولم يجب أحد، لا تظهر أي رسالة تنبيه (وهذا مقبول)، لكن يجب ألا يتم تشغيل المؤقت مرة أخرى.

🧩 المشاكل والتعديلات المطلوبة في صفحة اللعب (PlayPage):
✅ السؤال الذي تم اختياره يجب أن لا يكون قابلًا للاختيار مرة أخرى.

يتم ذلك إما بتلوينه أو بتعطيل زر السؤال بناءً على isAnswered = true.

✅ يجب تحديث حالة الأسئلة isAnswered عند العودة من صفحة السؤال.

الحل الحالي باستخدام window.addEventListener('focus') جيد، لكن تأكد من تحديث questions داخل fetchGameDetails.

✅ بعد انتهاء الإجابة يجب أن يتغير اسم الفريق (الدور) تلقائيًا.

تأكد من أن currentTeamIndex يتم تحديثه عند العودة من صفحة السؤال.

يمكن تحسين ذلك باستخدام useEffect لمراقبة document.visibilityState.

✅ إذا انتهت كل الأسئلة، يجب عرض رسالة تنبيه أو إنهاء اللعبة تلقائيًا.

تحقق من حالة كل الأسئلة داخل questions وإذا كانت جميعها isAnswered = true، يتم الانتقال إلى /game-result/{gameId}.

✅ يُفضّل تمييز الأسئلة التي تم الإجابة عليها بلون رمادي أو بإزالة التفاعل منها.

✅ يفضل إظهار إحصائية بسيطة مثل: "تبقى 3 أسئلة غير مجابة" أسفل الفئة.

🔒 ملاحظات عامة للمبرمج:
لا تجعل المؤقت يعيد التشغيل بعد آخر فريق.

لا تعتمد على setTimeout(() => startTimer()) بدون فحص حالة الفريق.

تأكد من أن جميع API التي تغير حالة الدور أو الإجابة تؤثر على قاعدة البيانات ويتم استرجاعها في PlayPage.

تأكد من أن game.questions يتم تحديثها بشكل مباشر بعد كل إجابة، وأن isAnswered يتم ضبطها بدقة.

