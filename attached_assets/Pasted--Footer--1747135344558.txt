✅ المهمة: بناء تذييل الموقع (Footer) قابل للتحكم من لوحة التحكم – مشروع "جاوب"
🎯 الهدف:
إنشاء تذييل (Footer) يظهر أسفل جميع الصفحات، ويحتوي على روابط ومحتوى قابل للتعديل بالكامل من لوحة التحكم دون الحاجة لتعديل الكود.

✅ عناصر التذييل المطلوبة:
1. روابط ثابتة (قابلة للتحكم):
من نحن

اتصل بنا

سياسة الخصوصية

الشروط والأحكام

الأسئلة الشائعة

تغيير اللغة (عربي | English)

2. سطر الحقوق:
نص مثل:
© 2025 جاوب - جميع الحقوق محفوظة

يكون قابل للتعديل أيضًا من لوحة التحكم.

3. روابط تواصل (اختياري):
روابط إلى: تويتر / واتساب / تيليجرام / إنستغرام

يتم إدارتها من لوحة التحكم أيضًا.

✅ التحكم من لوحة التحكم:
API:
API مثل: GET /api/footer-settings يُعيد بيانات التذييل

يجب أن يحتوي على:

json
Copy
Edit
{
  "links": [
    { "label": "من نحن", "url": "/about" },
    { "label": "اتصل بنا", "url": "/contact" },
    { "label": "سياسة الخصوصية", "url": "/privacy" },
    ...
  ],
  "socialLinks": {
    "twitter": "https://twitter.com/jaweb",
    "whatsapp": "https://wa.me/xxxxx",
    ...
  },
  "copyright": "© 2025 جاوب - جميع الحقوق محفوظة"
}
واجهة في لوحة التحكم تشمل:
إضافة / تعديل / حذف روابط التذييل.

تعديل سطر الحقوق.

رفع أيقونات التواصل الاجتماعي + الروابط.

✅ مثال JSX (React + Tailwind):
tsx
Copy
Edit
<footer className="bg-gray-100 text-center text-sm text-gray-600 py-6 mt-12 border-t">
  <div className="flex flex-wrap justify-center gap-6 mb-4">
    {footerLinks.map((link) => (
      <a key={link.url} href={link.url} className="hover:underline">
        {link.label}
      </a>
    ))}
  </div>

  <div className="flex justify-center gap-4 mb-2">
    {footerSocial.twitter && (
      <a href={footerSocial.twitter} target="_blank" rel="noreferrer">
        <img src="/icons/twitter.svg" alt="Twitter" className="h-5" />
      </a>
    )}
    {footerSocial.whatsapp && (
      <a href={footerSocial.whatsapp} target="_blank" rel="noreferrer">
        <img src="/icons/whatsapp.svg" alt="WhatsApp" className="h-5" />
      </a>
    )}
  </div>

  <div className="text-xs text-gray-500">
    {footerCopyright || "© 2025 جاوب - جميع الحقوق محفوظة"}
  </div>
</footer>
